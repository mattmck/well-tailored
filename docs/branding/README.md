
  # Create Branding Components

  This is a code bundle for Create Branding Components. The original project is available at https://www.figma.com/design/BgOkHOviHib4Ha7Hgce2s1/Create-Branding-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  